services:
  mentor:
    build: .
    container_name: coding-mentor
    environment:
      - LOCAL_UID=${LOCAL_UID:-1000}
      - LOCAL_GID=${LOCAL_GID:-1000}
    volumes:
      - .:/workspace
    working_dir: /workspace
    stdin_open: true
    tty: true
